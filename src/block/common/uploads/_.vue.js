
Vue.component('common-uploads', {

	template : '#common-uploads',

	mixins : [
		vueMixins.common,
	],

	data : function () {
		return {
			//items : this.$store.state.uploads.uploads_in_process,
		}
	},

});